# Local Claude Code context / settings (machine-specific, may reference internal paths)
CLAUDE.local.md
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# doc2md run artifacts (in case output lands inside the repo)
/markdown/
conversion_report.json
*_figures/

# Local-only tests: realistic vendor-shaped (sanitized) config fixtures we keep
# out of the public repo — e.g. Akamai Property Manager metadata XML. The
# committed suite stays vendor-neutral; run these with `pytest tests/local/`.
tests/local/
